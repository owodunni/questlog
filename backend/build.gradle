plugins {
  id "questlog.java-conventions"
}

version "0.1.0"

configurations {
  implementation {
    canBeConsumed = false
    canBeResolved = true
  }
}

dependencies {
  implementation(project(path: ":api", configuration: 'generatedApi'))
}

task assembleBackend(){
  //dependsOn copyGeneratedApi
}

task assemble{
  dependsOn assembleBackend
}